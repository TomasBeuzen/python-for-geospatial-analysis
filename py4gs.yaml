# install with `conda env create -fpy4ds.yaml`

name: py4ds
channels:
  - conda-forge
  - defaults
  - plotly
dependencies:
  - python=3.8
  - jupyterlab
  - matplotlib
  - pandas
  - numpy
  - geopandas
  - pykrige
